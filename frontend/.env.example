# Alfredo Frontend — Environment Variables
#
# Copy to .env and customize:
#   cp .env.example .env

# Backend API URL (include /api/v1 prefix)
VITE_API_URL=http://localhost:8000/api/v1

# Sentry (frontend error tracking — optional in dev)
VITE_SENTRY_DSN=
